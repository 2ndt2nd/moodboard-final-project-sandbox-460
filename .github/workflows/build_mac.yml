name: Build macOS App

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: 'pip'

      - name: Install system dependencies
        run: |
          brew install libpng jpeg

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip wheel
          pip install py2app==0.28.6
          pip install PyQt5==5.15.9
          pip install --pre torch==2.1.0 --extra-index-url https://download.pytorch.org/whl/nightly/cpu
          pip install git+https://github.com/openai/CLIP.git
          pip install tqdm==4.66.1 pillow==10.0.1

      - name: Create optimized setup.py
        run: |
          cat << 'EOF' > setup.py
          import sys
          sys.setrecursionlimit(5000)
          
          from setuptools import setup

          # Explicit imports for py2app
          import PyQt5.QtCore
          import PyQt5.QtGui
          import PyQt5.QtWidgets
          import PyQt5.QtSvg
          import PIL.Image

          APP = ['qt_app.py']
          DATA_FILES = []
          OPTIONS = {
              'argv_emulation': False,
              'packages': ['PyQt5', 'torch', 'clip', 'tqdm', 'PIL'],
              'includes': [
                  'numpy',  # Required by torch
                  'PIL.Image',
                  'PIL.ImageDraw',
                  'PIL.ImageFont'
              ],
              'excludes': [
                  'tkinter',
                  'matplotlib',  # Common unnecessary heavy dependency
                  'scipy'       # Often pulled in but not needed
              ],
              'resources': [],
              'compressed': True,
              'optimize': 2,
              'plist': {
                  'CFBundleName': 'MoodForager',
                  'CFBundleShortVersionString': '1.0.0',
                  'CFBundleIdentifier': 'com.yourdomain.moodforager',
                  'NSRequiresAquaSystemAppearance': False,
                  'LSMinimumSystemVersion': '10.15'  # Catalina or newer
              }
          }

          setup(
              app=APP,
              data_files=DATA_FILES,
              options={'py2app': OPTIONS},
              setup_requires=['py2app'],
          )
          EOF

      - name: Build application
        run: |
          # Clean any previous builds
          rm -rf build dist
          
          # Build with py2app
          python setup.py py2app --no-strip
          
          # Fix Qt plugin paths
          find dist/MoodForager.app/Contents -name "Qt*" -exec install_name_tool -add_rpath @executable_path/../Frameworks {} \;

      - name: Verify bundle structure
        run: |
          ls -la dist/MoodForager.app/Contents/MacOS/
          ls -la dist/MoodForager.app/Contents/Frameworks/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: MoodForager-Mac-Bundle
          path: dist/MoodForager.app
          retention-days: 7
